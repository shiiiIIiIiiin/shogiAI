# 開発履歴メモ（ShogiAlgo）

## 目的

- WebとUSI両対応の将棋AIを作る
- 友人配布用の .exe も作る
- Floodgateで対局して強さを検証する

---

## 主な変更の流れ

### Phase 1: 最小構成のエンジン（Python版）

- `Negamax + Alpha-Beta` を導入
- 評価は駒割りのみ（高速化優先）
- 静止探索（Quiescence Search）で取り合いを正確に評価
- 詰み探索（1〜2手詰み）を追加

### Phase 2: Web UI / USI対応

- Web: `web/app.py` + `web/templates/index.html`
- USI: `src/usi.py`
- 将棋所・Floodgateで動作確認

### Phase 3: C++移植（高速化）

- `cpp/main.cpp` に全ロジックを移植
- cshogiのC++ライブラリを静的リンク
- Python版の数十倍の速度を達成
- 飛車の駒評価が金の点数になっていたバグを修正

### Phase 4: 反復深化 + 時間制御

- 固定depth=5 → 反復深化（時間制限内で可能な限り深く）
- Floodgateのフィッシャー/秒読み/切れ負けに対応
- 1024ノードごとに経過時間チェック

### Phase 5: 評価関数の強化

- **詰み評価バグ修正**: `is_game_over()` 時に `evaluate()` ではなく `-999999` を返すように修正
- **PST（位置評価テーブル）追加**: 各駒種に位置ボーナスを付与
  - 飛車: 前進で大きなボーナス（`(7-rank)*7`）→ 飛車の活用を促進
  - 王: 自陣奥+端寄りでボーナス → 囲いを評価
  - 角: 中央寄りでボーナス
  - 歩・銀・桂: 前進+中央筋ボーナス
- **玉の安全度**: 隣接8マスの味方駒数を評価（飛車・龍は除外）
- **ミニ定跡**: 序盤3手を固定（先手: 7六歩→2六歩→2五歩、後手: 3四歩→8四歩→8五歩）
- **序盤の時間節約**: 30手目までは使用時間を30〜100%に線形削減、中終盤に配分

### Phase 6: 評価関数チューニング（現在）

- **King PST調整**: 王のボーナスが大きすぎて王自身が動き回る問題を修正
  - 最大50点 → 最大12点に縮小（自陣奥+7, 端寄り+5）
  - 前進ペナルティも緩和（`-(6-rank)*2`）
  - 王の移動インセンティブを抑え、金銀の配置（kingSafety）で間接的に囲いを評価
- **定跡削除**: ミニ定跡を全削除し、序盤から全て探索で判断する方式に変更
  - 定跡は相手の手に関係なく固定手を指すため、柔軟性に欠ける
  - 飛車PSTのボーナスが十分強いため、定跡なしでも飛車を活用できる

---

## 失敗・注意点（再発防止）

### 1. `is_checkmate()` が存在しない問題

- cshogi環境によっては `board.is_checkmate()` が無い
- 代替は「合法手ゼロ」の判定（`is_game_over()`）

### 2. 二重フォルダ問題（SHOGIAI / shogiAI）

- 将棋所の実行パスが古いフォルダを指していた
- 変更が反映されず、古いバグが再発

### 3. 詰み評価のバグ

- `is_game_over()` で `evaluate()` を返していたため、詰みを正しく評価できなかった
- 修正: `-999999 + (30 - depth)` を返す（浅い詰みを優先）

### 4. 深い探索と評価精度のトレードオフ

- 駒割りのみの評価で深く読むと、かえって弱くなる（search pathology）
- PST追加で評価関数が重くなり、到達深さが7→5に低下
- 玉の安全度を2マス範囲→1マス範囲に戻して深さを回復

### 5. 飛車の活用不足

- 序盤で飛車先の歩を突かないため飛車が活用されなかった
- PSTの飛車前進ボーナス（`(7-rank)*7`）で対処

### 6. exeロック問題

- 将棋所が ShogiAlgo.exe を使用中はビルドできない（LNK1104エラー）
- ビルド前に将棋所を閉じる必要がある

### 7. King PSTが大きすぎる問題

- 王の位置ボーナスが大きいと、王自身が率先して囲い位置に移動してしまう
- 金銀が動かなくなり、不自然な序盤になる
- 対策: King PSTを最大12点まで縮小し、kingSafetyとの併用で囲いを評価

### 8. 定跡の柔軟性問題

- 固定手順の定跡は相手の攻めに対応できない
- 囲い構築を定跡に入れると、相手に攻められても囲い手順を続けてしまう
- 対策: 定跡を全削除し、評価関数（PST + kingSafety）に任せる方式に変更

---

## これからの方針

- Floodgateでの対局結果を見て評価関数を調整
- 必要に応じてPSTの値を微調整
- 探索の高速化（置換表等）を検討
