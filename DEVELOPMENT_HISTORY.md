# 開発履歴メモ（ShogiAlgo）

## 目的

- WebとUSI両対応の将棋AIを作る
- 友人配布用の .exe も作る
- 反則手を出さないことを最優先で検証する

---

## 主な変更の流れ

### 1. 最小構成のエンジン

- `Negamax + Alpha-Beta` を導入
- 評価は駒割りのみ（高速化優先）
- ここで反則手が出ることがあった

### 2. 静止探索の導入

- `depth=0` の評価を capture-only で伸ばす
- 戦術は良くなるが、探索量が一気に増えやすい

### 3. Web UI / USI対応

- Web: [web/app.py](web/app.py) + [web/templates/index.html](web/templates/index.html)
- USI: [src/usi.py](src/usi.py)
- UIでの動作確認を優先

### 4. 反則手対応

- `board.is_legal()` で最終手チェック
- ダメなら最初の合法手にフォールバック
- ログ出力を強化（[shogialgo_debug.log](shogialgo_debug.log)）

### 5. 詰み探索（先読み）

- `ShogiEngine.mate_search_depth` を追加
- 先読み詰みが見つかれば即返す方式

---

## 失敗・注意点（再発防止）

### 1. `is_checkmate()` が存在しない問題

- cshogi環境によっては `board.is_checkmate()` が無い
- 代替は「王手中かつ合法手ゼロ」の判定

### 2. 二重フォルダ問題（SHOGIAI / shogiAI）

- 将棋所の実行パスが古いフォルダを指していた
- 変更が反映されず、古いバグが再発

### 3. 詰み探索が重い局面

- 終盤の分岐増加で時間が急増
- 先読み深さは小さく保つ（例: 1〜2）

---

## これからの方針

- 反則手ゼロを最優先
- 詰み探索は軽量な範囲でのみ実施
- Web/USIの動作パスを常に同じに保つ

---

## 高速化の検討メモ

現状、1手あたり5分かかる局面があるため、以下の方針を検討中。

- C++化による高速化
- αβ探索の効率化（手の並び替え強化）
- 秒数制限の導入
- 「激戦区ポイント」を定義して、
  - そこから遠い駒の探索を粗くする
  - 飛び道具（飛車/角/香）は別扱いで読む

---

## 2026-02-17 高速化の実装メモ

- 反復深化 + 秒数制限（USI/Web）を導入
- 手の並び替えを強化（MVV-LVA相当 + 王手優先）
- Quiet手の後半にLMR（探索深さを1手減らす）

### 注意点（再発防止）

- **制限時間が短すぎると弱くなる**
  - 対策: USIの`byoyomi`や残り時間から上限2秒/下限0.2秒で配分
- **USIで時間情報が来ない場合の暴走**
  - 対策: デフォルト2秒の固定制限
- **探索打ち切り時に手が返らない**
  - 対策: 反復深化で「最後に完走できた深さ」の最善手を採用

---

## 2026-02-18 C++化による高速化

### 背景

Python版は1手5分かかる局面があり、Floodgate等の対局サーバーで使うには遅すぎた。
cshogiのC++ライブラリ（盤面管理・合法手生成）を活用し、探索ロジックをC++に移植。

### 実施内容

1. **C++ USIエンジンの新規作成**
   - [cpp/main.cpp](cpp/main.cpp): エンジン本体（Negamax + 静止探索 + 詰み探索 + USIループ）
   - [cpp/CMakeLists.txt](cpp/CMakeLists.txt): ビルド設定
   - cshogiのC++ライブラリ（`cpp/cshogi/src/`）を静的ライブラリとしてリンク
   - 出力: `cpp/build/Release/ShogiAlgo.exe`

2. **反復深化 + 時間制御の実装**
   - depth 1から順に深くし、時間内に完了した最深の結果を採用
   - 1024ノードごとに時間チェック、超過時は即座に打ち切り
   - 次の深さに進むかは経過時間が制限の60%以内かで判断

3. **Floodgate対応の時間配分**
   - フィッシャー（10分+10秒加算）: 加算時間を基本に残り時間の一部を上乗せ
   - 秒読み: 残り時間/50 + 秒読みの80%
   - 切れ負け: 残り時間/30
   - 時間情報なし: デフォルト2秒

4. **Python版のバグ修正**
   - `engine.py` の持ち駒評価で金/角/飛の値がずれていた
   - 原因: `pieces_in_hand`の順序（HandPiece順）とPieceType順の違い
   - HandPiece: [歩,香,桂,銀,**金**,角,飛] / PieceType: [歩,香,桂,銀,**角**,飛,金]
   - 修正: 正しいPieceType値のマッピングテーブルを使用

5. **cshogiビルドエラーの修正**
   - `usi.cpp` のデバッグ用コードが `NDEBUG` 定義時にコンパイルエラーになる問題を修正

### 注意点（再発防止）

- **cshogiを`git clone`で持ってくると`.git`が入れ子になる**
  - 対策: `cpp/cshogi/.git` を削除してからメインリポジトリにadd
- **HandPieceとPieceTypeの順番の違い**
  - HandPiece: HPawn=0, HLance=1, HKnight=2, HSilver=3, **HGold=4**, HBishop=5, HRook=6
  - PieceType: Pawn=1, Lance=2, Knight=3, Silver=4, **Bishop=5**, Rook=6, **Gold=7**
  - 持ち駒を評価する際は必ず変換テーブルを使うこと
- **フィッシャー時間制御で加算時間以下しか考えないと弱くなる**
  - 対策: 残り時間に余裕があれば加算時間分はフルに使う
